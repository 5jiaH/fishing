# MySQL 连接请统一在 .env 中配置（本文件勿再写 MYSQL_*，避免覆盖成 root）

# 游客无 JWT 访问受保护 test 接口：同一 IP 当日允许次数（默认与 .env 一致，可按环境覆盖）
API_GUEST_RESTRICTED_MAX=3

SIGNOZ_API_KEY='KU6ZL0H6D5IR4gthZoKW9vMIkm31GkVn1h9YRNWK85U='
SIGNOZ_API=http://192.168.163.139:8080/api
SIGNOZ_SOURCEMAP_PATH=E:/SourceMap