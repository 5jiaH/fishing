When handling frontend UI/page/component tasks:
1. Use the `frontend-design` skill first.
2. Apply `DESIGN.md` as the source of truth for tokens and style rules.
3. If the skill is unavailable, explicitly say so and continue using `DESIGN.md`.